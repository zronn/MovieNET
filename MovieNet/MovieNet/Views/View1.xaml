<UserControl x:Class="MovieNet.Views.View1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieNet.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label Content="{Binding Label}" HorizontalAlignment="Left" Margin="294,24,0,0" VerticalAlignment="Top"/>

        <DataGrid x:Name="ListMovie" ItemsSource="{Binding Movies}"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  HorizontalAlignment="Left" Height="260" Margin="153,80,0,0" VerticalAlignment="Top"
                  AutoGenerateColumns="False" EnableRowVirtualization="True" IsReadOnly="True" Width="414">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="IdColumn" Binding="{Binding Id}" 
                                    Header="Id" Width="Auto"/>
                <DataGridTextColumn x:Name="TitleColumn" Binding="{Binding Title}" 
                                    Header="Title" Width="Auto"/>
                <DataGridTextColumn x:Name="typeColumn" Binding="{Binding Type.Id}" 
                                    Header="Type" Width="Auto"/>
                <DataGridTextColumn x:Name="DescriptioneColumn" Binding="{Binding Description}" 
                                    Header="Description" Width="Auto"/>
                <DataGridTextColumn x:Name="UserColumn" Binding="{Binding User.Id}" 
                                    Header="User" Width="Auto"/>
                <DataGridTextColumn x:Name="NoteColumn" Binding="{Binding Note}"
                                    Header="Note" Width="Auto"/>
                <DataGridTextColumn x:Name="CommentColumn" Binding="{Binding Comment}" 
                                    Header="Comment" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
